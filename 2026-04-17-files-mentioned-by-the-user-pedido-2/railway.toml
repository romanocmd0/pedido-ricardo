[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'gunicorn --bind 0.0.0.0:${PORT:-8080} --access-logfile - --error-logfile - app:app'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
